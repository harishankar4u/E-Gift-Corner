@{
    ViewBag.Title = "ShoppingCart";
}


<h2>Shopping Cart</h2>
@{
    if (Session["Items"] != null)
    {
        var gifts = Session["Items"].ToString();
        @Html.Raw(gifts);
    
    }
    else
    {
        <h4>No items found.</h4>
    }
        
}

